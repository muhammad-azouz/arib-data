namespace AribONE.Models;

/// <summary>Where an order originated (tasks/spec-order-management.md, Data model).</summary>
public enum OrderChannel
{
    CallCenter = 1,
    Branch = 2,
    Sales = 3,
}
